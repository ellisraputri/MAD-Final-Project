[phases.setup]
nixPkgs = [
  "nodejs_22",
  "python311",
  "gcc",
  "ffmpeg",
  "glib",
  "libGL"
]

[phases.install]
cmds = [
  "npm install",
  "pip install -r requirements.txt"
]

[start]
cmd = "npm start"